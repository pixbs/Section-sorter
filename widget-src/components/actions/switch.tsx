const { widget } = figma;
const { AutoLayout, Text } = widget;

function Switch() {
    return (
        <AutoLayout>
            <Text>Switch</Text>
        </AutoLayout>
    )
}

export default Switch;