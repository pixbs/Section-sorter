const { widget } = figma;
const { AutoLayout, Text } = widget;

function Update() {
    return (
        <AutoLayout>
            <Text>Update</Text>
        </AutoLayout>
    )
}

export default Update;